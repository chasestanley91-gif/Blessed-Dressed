# Handoff — 2026-08-01 session B (spec-as-law ingest + pipeline proof)

Hand-maintained; supplements `HANDOFF-2026-08-01.md`. `STATE.md`/`CONTINUE.md` regenerate.

## THE RULING THAT CHANGES EVERYTHING

**The owner's drafting specifications are LAW. Illustrations and legacy descriptions AID,
they do not direct.** Recorded with full pipeline consequences in
`data-store/drafting-specs/README.md`. The spec library (12 documents, all six products,
transcribed verbatim from the owner) lives in `data-store/drafting-specs/` and is committed.
Conflicts go in `spec-conflicts.log.md` (2 logged: slant-N semantics, shawl foot data).

## What happened this session

1. **Phase 0 unblock, 0 credits.** `publish_approved.mjs` folder scan now prefers the
   SHA-matching folder (was: first-existing — jacket/ legacy files shadowed suit-3pc/
   masters). `project_state.mjs` join is now field- AND section-aware via spec.json
   (sb-5 phantom + coin-pocket section leak fixed). Primary fan-out row now chosen by
   spec `sectionId`. **PASS_NOT_SHIPPED 6 → 0**: shipped db-6x2, db-6x3, sb-4 (suit-3pc),
   coin-none + coin-left (suit-2pc → also suit-3pc).
2. **Spec ingest.** 320 catalog descriptions across 109 options (lapels 26, trousers 76,
   vest 7) via `set_description.mjs`; edit files in `data-store/drafting-specs/edits/`.
   All 47 touched pipeline records re-extracted (orientation reused from spec `view`) and
   prompts rebuilt; geometry verified reaching `requiredTokens` and the prompt string.
   validate_prompt 192/193 (1 pre-existing missing SVG: `lbh-none`).
   - **Excluded ids, do NOT bulk-edit:** `coin-*` (three contexts incl. the jacket coin
     pocket) and `db-6x2` (jacket stance vs vest stance). They need per-section handling.
   - Extractor: on-seam SHAPES rule tightened ('side seam' as datum ≠ on-seam pocket);
     snapshot synced. Watch `classifyExcluded`: bare "fabric"/"lining" with no
     placement keyword excludes an option — write "cloth" and include "depth/placed/width".
3. **Pipeline proof with credits (the goal's mandatory pre-batch test) — PASSED.**
   - `suit-2pc/lapel-shawl-0e`: PASS attempt 1, 9×98, published with fan-out
     (sport-coat, suit-2pc, suit-3pc), replacing the ca22563b317d collision copy.
   - `suit-2pc/lapel-shawl-0005`: FAIL attempt 1 (crop hid the narrow-foot discriminator)
     → descriptive correction → PASS attempt 2, published with fan-out.
   - 3 generations, 2 shipped, 1.5 credits → **1.5 attempts/shipped** (vs 1.75 historical).
   - Shawl collision: 6 → 4 (`lapel-shawl`, `-d`, `-0a`, `-asymmetric` remain).
4. Stale "waiver not implemented" note corrected in CHECKPOINT.json (it IS implemented).

## Position: 119 / 1,759 shipped. Credits ≈ 793.

## Next actions, priority order

1. **Continue the shawl family** — `lapel-shawl` (generic, has the radius-progression spec
   2/4/7 cm), `-d`, `-asymmetric` have spec text in the catalog; 0a needs its blueprint
   question resolved (its drawing is byte-shared across the family per collision-triage).
2. **Peak/notch ladders** — specs now give per-rung projections (0.3–0.5 cm steps). Shoot
   as a SET, gate with `tools/qc_ladder.mjs` blind ordering; report failed rung pairs to
   the owner with measurements rather than shipping near-duplicates.
3. **16 spec-unblocked no-blueprint options** (wband-35..50, pocket-depth, leg-shape,
   waist-*) — generate from spec alone; illustration slot empty is ACCEPTABLE for these
   under the ruling (validate_prompt needs `--allow-missing-illustration`).
4. **Shirt collar/cuff spec mapping** — compositional id taxonomy (`cuff-sq-2btn-1bh`,
   `cw-5..8` = the spec's 5–8 cm heights, `cuf-lining-soft/normal/hard` = interlining).
   Map spec geometry blocks onto ids, then set_description + re-extract.
5. **Legacy grading** (0 credits) — `build_grading_batches.mjs --write`, read-only agents,
   record via `log_legacy_audit.mjs`. 133 graded so far (91 OK / 26 WRONG / 16 UNSURE).
6. **baoxiniao harvest** — `download-options.mjs` needs `FACTORY_USER`/`FACTORY_PASS`
   (NOT in env; owner must supply). Targets the remaining ~85 missing drawings + loops-5.

## Wave discipline (learned, do not relearn)

- Batch `media_upload({files:[...]})`/`media_confirm({media_ids:[...]})` per wave.
- `medias` mandatory on EVERY generate, retries included. gpt_image_2 role is `image`.
- Log QC before regenerating (attempt counter lives in qc.json).
- **New lesson:** when the discriminator lives at the BOTTOM of an option (foot,
  termination, crossing, hem), the base chest-up framing crops it. Frame waist-up for
  any option whose spec names such a feature — consider teaching build_prompt this.
