# Run state — 2026-08-12 (V4 phases 1–7, stopped at generation gate)

Resumable checkpoint. Re-derive everything below with:

```
node tools/build_decision_ledger.mjs --write
node tools/generation_queue.mjs
node tools/catalog_invariants.mjs          # must print 2862 / all invariants hold
```

## What this run did

1. **Corrected the project path.** The `/ship-craft` ground-truth block pointed at
   `C:\Users\ChaseStanley\Downloads\blessed-dressed`, a stale May/June copy with no
   decision files, no `src/app`, and no git. The canonical project is
   `C:\Users\ChaseStanley\Downloads\files\brand_assets\blessed-dressed` (git-tracked).
   All numbers in the earlier audit-only report were measured against the stale copy
   and are void.
2. **Rebuilt the canonical ledger** (Phase 2). It was built 2026-08-11 18:31 but the
   owner made 151 more decisions in the admin UI today (66 approved / 85 rejected),
   so every downstream tool was reading a stale ledger.
   Result: 533/533 admin decisions bound to a full craft address, **0 true conflicts,
   0 unresolved references**.
3. **Recomputed the generation queue** (Phase 6) and **ran the invariant gate**
   (Phase 11 partial): 2862 options, 1533 referenced assets, 2 allowlisted missing,
   `all invariants hold`, exit 0.
4. **Safety backup** of the catalog at `data-store/options.prebackup-2026-08-12-approvals`.

## Queue state now vs the 2026-08-11 audit

| State | 08-11 | now | movement |
|---|--:|--:|---|
| X excluded (swatch/thread/button) | 1540 | 1540 | — |
| A missing/suspect illustration | 80 | 80 | blocked on source material |
| B needs fresh generation | 4 | 4 | — |
| C replace rejected (failure-aware) | 268 | **320** | +52 — owner rejected 85 more today |
| D awaiting owner approval | 153 | **69** | −84 — owner ruled on them |
| E complete | 817 | **849** | +32 |

Generation work outstanding: **B(4) + C(320) = 324 crafts.**

## BLOCKED — needs the owner

### 1. `apply_owner_approvals.mjs --apply` was denied by the permission classifier

Dry run is clean and waiting: **263 approvals already live exactly, 62 to enact
(5 publish + 57 rewire), 0 flagged as needing a human, 208 rejections to fold into
retry prompts.**

This single command is the whole of Phase 4 (restore approvals), Phase 10 (publish
approved), and the write half of Phase 7 (fold rejection reasons into prompts).
Until it runs:

- 62 images the owner approved in the UI are **not live** on the crafts they were
  approved for;
- only **87 of 299** prompt.json files carry an `OWNER CORRECTION` block, so 208
  rejection reasons are recorded but not yet encoded into the retry prompts —
  regenerating now would violate V4 §7 ("never simply regenerate the same prompt").

**Therefore Phase 8 generation must not start before this command runs.**

### 2. Generation budget gate (V4 §16 / ship-craft rule 3)

- Estimate: 324 × 0.5 credits × 1.5 = **~243 credits**
- Balance at this run: **623.88 credits** (plus plan) — affordable
- Not started. Needs an explicit go.

### 3. 36 C-state crafts should NOT be regenerated from their current drawing

Top rejection reasons across the 320 C crafts:

| n | reason |
|--:|---|
| 57 | Unrealistic / AI look |
| 36 | **Reference drawing itself is wrong** |
| 23 | Wrong shape / outline |
| 13 | Wrong side or orientation |
| 11 | Wrong size or proportion |
| 8 | Wrong garment or wrong part |

The 36 "reference drawing itself is wrong" crafts are mis-stated as state C. Their
defect is upstream: generating again from the same bad drawing reproduces the
failure. They belong in state A (needs a correct source illustration) and should be
split out of the generation wave, per V4 §3 ("if an illustration cannot be
confidently verified, do not silently use it") and the §17 conflict rule.

## Session 2 — owner-supplied reference images (the "correct illustration" uploads)

The owner said he had added the correct illustrations as reference images. Measured:
**238 files under `public/images/review-refs/`** (111 of them new since 2026-08-11),
attached to **133 decisions**, resolving to **137 craft options**. Every file exists on
disk and every one binds to a full craft address.

**Nothing consumed them.** `/api/admin/image-review/reference` stored the uploads and no
tool ever read them, so these corrections had no effect on any prompt or illustration.
That is now wired up:

| New/changed | What it does |
|---|---|
| `tools/promote_owner_references.mjs` | Promotes a verified drawing into `techpackIllustration` + `illustration`; refuses photos, SVGs, missing files; logs every replaced path to append-only `data-store/illustration-history.json`; marks affected specs stale. |
| `tools/reinterpret_changed_illustrations.mjs` | Re-runs tech-pack-interpreter for crafts whose drawing was replaced, and only clears the stale marker once the fresh spec names the new drawing. |
| `garment-image-director/scripts/build_prompt.mjs` | Now emits an `OWNER REFERENCE PHOTOGRAPHS` block + `ownerReferences[]` whenever `owner-references.json` exists. Fixture-tested both ways; `validate_prompt.mjs` still exits 0. |
| `garment-image-director/SKILL.md` | New step 4b — attach owner photos as ADDITIONAL `medias` beside the illustration, never instead of it. |
| `src/app/admin/image-review/page.tsx` | Shows the full `product > section > field > option` address instead of the ambiguous `product/option` (the `coin-left` collision class). Typecheck + `npm run build` pass. |

### The finding that reframes the work

The uploads are overwhelmingly **real photographs**, not line-art tech packs. Early
verification: of the first 31 items classified, ~23 are `geometry-reference-only`
(photographs of the correct craft), only 2 are promotable drawings.

A photograph cannot be image #1 — `catalog_invariants` check 7 fails an illustration in
a photo path, and V4 §3 bars photographs from that slot. So most of these route to
generation guidance rather than illustration promotion.

**Open question for the owner:** several of these photos show the craft correctly in real
cloth. Publishing an owner photo directly would cost 0 credits and carry no hallucination
risk — strictly better than generating an imitation of it, and it is the same argument
`tools/scan_reference_photos.mjs` already makes. Needs an explicit owner decision because
§14 forbids anything unapproved going live.

### Structural defect found and fixed: "failure-aware" retries were not failure-aware

The owner's rejection reasons were stored **only** as text appended to `prompt.json` after
the fact (`apply_owner_approvals.mjs` → `OWNER CORRECTION AFTER REVIEW` + a structured
`ownerCorrections[]`). `build_prompt.mjs` knew nothing about them. Two consequences:

1. Any `build_prompt.mjs --write` rebuild silently erased the block. The only guard was a
   prose warning to humans in `generation_wave.workflow.js:49`.
2. Worse, `tools/prep_batch.mjs` generates from a **freshly built** prompt
   (`build_prompt.mjs --json`, never `--write`), which therefore **never contained the
   correction at all**. A state-C retry driven through prep_batch would have regenerated
   with no knowledge of why the owner rejected it — a direct V4 §7 violation, and a likely
   cause of repeat rejections for the same reason.

**Fix:** `build_prompt.mjs` now carries `ownerCorrections[]` forward and re-emits the block
as a DERIVED part of every build, appended last so it still outranks earlier lines. No
rebuild can drop it, and every consumer (including prep_batch's in-memory build) gets it.

Verified on `shirt/bias-outer-top-collar` (1 existing correction):

- fresh `--json` build → `ownerCorrections: 1`, exactly 1 block in the prompt text
- two consecutive `--write` rebuilds → still 1 and 1 (idempotent, no duplication)
- `validate_prompt.mjs` → `1/1 option(s) passed the pre-flight gate`, exit 0
- test file restored to its committed bytes afterwards

### Pre-generation note: prompts are rebuilt per wave (staleness is handled)

**297 of 299 `prompt.json` files predate the current template logic** (`lib/prompt.mjs`,
2026-08-08); 110 date to July. Rebuilding one (shirt/collar-regular-20) produced a
materially different prompt (117 insertions / 133 deletions).

This is **not** a blocker: `tools/prep_batch.mjs` re-runs `extract_spec.mjs --write` and
then builds the prompt fresh for every craft it prepares, so a wave never generates from a
stale on-disk prompt. Combined with the correction-carry-forward fix above, a fresh build
now includes the owner's rejection reasons too — which is what made the rebuild safe to
rely on in the first place.

Still required, in this order, for any craft whose illustration changed:
promote → re-interpret spec (`reinterpret_changed_illustrations.mjs --apply`) →
prep_batch (rebuilds prompt + validates) → generate.

## Session 2 final state (all verification complete)

221 reference images were opened and judged; all 23 promotion candidates were re-checked by
an independent skeptic instructed to refute them.

| Outcome | Count |
|---|--:|
| Usable as generation reference (attached) | 100 items → 57 crafts |
| Promoted to illustration (survived refutation) | **3** |
| Refuted promotion candidates | 20 of 23 |
| Do not depict their craft | 50 |
| Cannot be judged safely | 68 |

**The 3 promotions** (`data-store/illustration-history.json` records every replaced value):

1. `suit-2pc|suit-pockets|lower-pocket|lp-large-slanted-60` — had **no** illustration → 186×186 line art dimensioned "6cm".
2. `sport-coat|suit-pockets|lower-pocket|lp-patch-flap-btn` — had **no** illustration → 214×225 patch-pocket component drawing.
3. `suit-3pc|lapel|lapel-style|lapel-peak-101` — replaced a 1200×1200 supplier plate that **33 different peak-lapel crafts (101°–120°, curved and straight) all shared**. That plate is the "generic category illustration" §3 forbids and a §20 shared-image defect; the 155×242 upload is annotated "101°" and is craft-specific. Identity beat resolution — but it should be redrawn at higher resolution.

All three re-extracted through tech-pack-interpreter with orientations confirmed by actually
viewing each drawing (front / front / detail) and now sit in state C with a usable drawing.
Invariants: `options 2862 | referenced local assets 1536 | all invariants hold`, exit 0.

### Queue-state defect found and fixed

`generation_queue.mjs` folded `spec.generate === false` into state X. Because
`generate = hasBlueprint && !excluded`, that filed **700 real craft options** — lower
pockets (23 per jacket product), lapel widths (17–20 per product), ticket pockets (11 per
product), lapel buttonhole styles, perfume pads — as "excluded from generation by design"
when their actual problem is that **no drawing exists on disk**. X now means only the
by-design swatch exclusion; a craft with no blueprint goes to A, which is what A already
documented itself to mean.

The audit's headline gap changes materially:

| State | Before | After | Meaning |
|---|--:|--:|---|
| X excluded by design | 1540 | **840** | fabric 246 + thread 353 + button 241 — genuine swatches |
| A blocked on missing source | 80 | **778** | needs a drawing before any generation is possible |
| B needs generation | 4 | 4 | |
| C failure-aware retry | 320 | **363** | |
| D awaiting owner | 153 | **0** | owner has now ruled on every queued candidate |
| E complete | 817 | **877** | |

The real "cannot proceed without source material" number is **778, not 80**. That is the
single largest piece of remaining work in the project and it was previously invisible.

### Owner decisions kept arriving during this run

Decisions grew 533 → **711** while working (117 approved, 61 rejected after the first
ledger rebuild). The ledger and queue were rebuilt to bind all 711 (0 conflicts, 0
unresolved). Consequence: **177 owner approvals are now pending publication** (was 62) and
**269 rejections are waiting to be folded into retry prompts** (was 208).

## Session 3 — PUBLISHED (owner said "publish", 2026-08-12)

`node tools/apply_owner_approvals.mjs --apply`

```
owner approvals already live exactly : 265
approvals to enact                   : 177
APPLIED: 5 published, 172 rewired, 10 corrections folded
FAILURES: 0
```

Verified, not assumed:

- All **177** served files exist on disk (checked each path).
- All **177** catalog rows now have `image` exactly equal to the approved image
  (compared craft-by-craft against the log's `served` path). 0 mismatches.
- Invariants after publish: `options 2862 | referenced local assets 1557 | all invariants
  hold`, exit 0. Referenced assets rose 1536 → 1557.
- Ledger rebuilt: 711/711 decisions bound, **0 conflicts, 0 unresolved**, 1883 crafts with
  owner photo approval.
- **Idempotence proven**: a second `--apply` reported `442 already live exactly / 0 to
  enact / 0 published, 0 rewired`. Nothing double-published.

### The 183 unfoldable corrections — fixed, not just logged

The first run folded only 10 of 269 rejection reasons; **183 were skipped because those
crafts have no `prompt.json` yet**. Skipping is not harmless: those are exactly the crafts
whose next generation would be their FIRST, with no record of what the owner rejected —
the §7 failure this project exists to stop.

Two changes make the correction independent of whether a prompt happens to exist:

1. `apply_owner_approvals.mjs` now **parks** an unfoldable correction in
   `.craft-pipeline/<product>/<option>/owner-corrections.json` (deduped on
   attemptRejected + decidedAt) instead of only logging the skip.
2. `garment-image-director/scripts/build_prompt.mjs` reads BOTH
   `prompt.json.ownerCorrections` and that parked file, merges and de-duplicates them, and
   emits the `OWNER CORRECTION AFTER REVIEW` block on **every** build — including the first
   one a craft ever gets.

Result: **183 corrections parked**, 0 of them on crafts that already had a prompt (the
invariant that parking only happens when there is nothing to append to).

Verified on `shirt/bh-hand`, a craft with NO prompt.json:

```
ownerCorrections merged: 1
OWNER CORRECTION block in prompt: true
"...attempt 4 was rejected by the owner — obey this over any conflicting earlier line,
 including the BLUEPRINT LOCK): What is wrong with it: Wrong shape / outline;
 Feature not visible / too small in frame..."
```

Every one of the owner's 269 rejection reasons now reaches its retry prompt: 86 folded into
existing prompts, 183 parked and folded on first build.

### State after publishing

| State | Count |
|---|--:|
| X excluded by design (swatch/thread/button) | 840 |
| A blocked on missing source material | 778 |
| B needs generation | 4 |
| C failure-aware retry | 363 |
| D awaiting owner | 0 |
| E complete | **877** |

Generation remains ungated-on-budget: **367 crafts (B+C) ≈ 275 credits**, balance 623.88.

## Live-environment note

A dev server is listening on :3000 and the admin UI is actively writing
`data-store/image-review-decisions{,-log}.json` (both mtime 06:16 today; the file
grew during this run). Any decision count here is a moving snapshot — rebuild the
ledger immediately before applying anything.
