# CONTINUE — start here

_Generated by `tools/project_state.mjs` at 2026-08-01T04:40:05.167Z. Re-run the tool after any work unit._

## Read first

1. `STATE.md` — current position and blocking findings
2. `public/images/reports/CHECKPOINT.json` — decisions already made, bugs already fixed, credit ledger
3. `public/images/reports/failure-log.md` — prompt lessons; **read before writing any prompt**
4. The pipeline skills, in order: `tech-pack-interpreter` → `garment-image-director` → `garment-image-qc`

## Standing rules

- The tech-pack illustration is law. Never improve it, never redesign it.
- `garment-image-qc` is the only authority that may approve a catalog write-back.
- Never overwrite an approved asset. Never generate from `/images/generated/` (that is our own output).
- A wrong blueprint is invisible to QC — the more faithful the render, the higher it scores. Check the drawing against the label before spending credits.
- Log every credit spend in `failure-log.md`.

## Next actions, in priority order

1. **Clear blocking `PASS_NOT_SHIPPED`** (6) — QC PASSed but the catalog does not point at a generated image. See STATE.md for the list.
2. **Clear blocking `SHIPPED_IMAGE_REUSED`** (1) — One generated image is wired to two or more DISTINCT craft options. See STATE.md for the list.
3. **Clear blocking `DISTINCT_OPTION_IMAGE_COLLISION`** (14) — Byte-identical images ship for craft options that must look different from each other. See STATE.md for the list.
4. **Re-verify 11 option(s)** whose approval was revoked (stage `needs-reverify` in repo-index.json).
5. **Run corrected retries** for 24 option(s) at QC FAIL — read the qc.json `correction` field first.
6. **QC 2 generated image(s)** that have no verdict yet (garment-image-qc).
7. **Generate 21 interpreted option(s)** that have a spec but no image (garment-image-director).
8. **Start 693 in-scope option(s)** with no artifacts at all (tech-pack-interpreter first).
9. **Decide the fate of 861 legacy image(s)** shipped with no qc.json — re-verify or replace.

## Queries against the index

```bash
node tools/project_state.mjs --audit            # findings only, writes nothing
node tools/project_state.mjs --json             # summary roll-up

# every option at a given stage
node -e "const r=require('./public/images/reports/repo-index.json').records; \
  console.log(r.filter(x=>x.stage==='not-started').map(x=>x.addr).join('\n'))"
```

## Open questions owed by the user

- RESOLVED 2026-07-30 (was: OPEN QUESTION FOR USER on the >=98 gate). User decision: the gate STAYS >=98; a 3rd attempt is allowed; after 3 attempts an image with ZERO critical/major findings may ship at >=95 as a LOGGED WAIVER in qc.json (verdict PASS_WAIVED, computed mechanically by log_qc_result.mjs, never asserted by the agent). Not yet implemented — see HANDOFF.md next action 4.
