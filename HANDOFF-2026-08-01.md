# Handoff — 2026-08-01

Standalone by design: `CONTINUE.md` is regenerated by `tools/project_state.mjs`, so anything written
there is lost on the next run. This file is hand-maintained.

---

## Run these three first

```bash
node tools/sync_pipeline_snapshot.mjs --check   # exits 1 if the live skills drifted from the repo copy
node tools/description_audit.mjs                # 2658 descriptions; the sharp hazards are actionable
node tools/log_legacy_audit.mjs --report        # 25 graded, 4 WRONG live images queued
```

`sync_pipeline_snapshot --check` matters most. The three skills that decide what every catalog image
depicts live in `~/.claude/skills/`, which **is not a git repository**.
`.claude/skills/_pipeline-snapshots/` is the only version history they have, and it goes stale the
moment a skill is edited.

---

## PIPELINE ORDER — getting this wrong wastes credits silently

```
tools/set_description.mjs                       edit the catalog by option id
  -> extract_spec.mjs --orientation=X --write   REQUIRED: spec.json caches the description
  -> build_prompt.mjs --write
  -> validate_prompt.mjs                        gates BEFORE any credit is spent
  -> generate -> record_generation.mjs
  -> grade -> tools/log_qc_batch.mjs            REQUIRED before regenerating
```

Both failure modes below happened on 2026-08-01, and both are silent:

1. **Skipping `extract_spec --write` after a description change.** `build_prompt` alone rebuilds from
   the cached copy. The "rebuilt" prompt came back byte-identical at 5007 chars, still containing a
   sentence that had just been deleted from the catalog.
2. **Regenerating without logging QC.** `record_generation.mjs` auto-increments the attempt number
   from any prior `qc.json`. With none logged, attempt 2 was written as attempt 1 and **overwrote
   candidate-1.png**. Recoverable from git (the prior `generation.json` carries the CDN URL), but the
   provenance chain breaks. Log QC first, or pass `--attempt` explicitly.

---

## Ready to run: 9 prepared generations, ~4.5 credits

Specs extracted, prompts built, all pass `validate_prompt`. Blueprints were uploaded and confirmed on
2026-08-01; if the media IDs have expired, re-uploading is free.

Ten distinct blueprints separate 22 colliding options that currently share **5** images:

| generate | fans out to |
|---|---|
| `lp-slanted-flap-55` | + `-65` — one shared drawing, a cm ladder |
| `lp-straight-jetted-40` | + `-45 -50 -55 -60 -65` — one shared drawing |
| `cp-welt-23` | + `cp-welt-25`, `-27` — one shared drawing |
| `cp-trapezoid` | **DONE**, attempt 2 correct direction, needs formal QC |
| `lapel-shawl-0e`, `lapel-shawl-0005` | genuinely distinct drawings |
| `lp-jetted-4` | + `lp-straight-jetted` — one shared drawing, merge candidate |
| `lp-jetted-btn-tab`, `lp-patch-flap`, `lp-patch-rounded` | genuinely distinct |

Held pending grader verdicts: `cp-jetted`, `lapel-shawl-0a`, `lp-patch`.

**Ladder rungs cannot be separated by regenerating** — they share one drawing. That is a merge/reprice
question, not a credit question.

---

## Measured facts worth not re-deriving

**Three shawl blueprints**, normalised to the neckband width present in all three. No centimetres are
claimed: none of the sheets carries a dimension callout.

| | 0a | 0e | 0005 |
|---|---|---|---|
| lapel width @20% → @75% of drop | 0.219 → 0.281 (widens) | 0.277 → 0.258 (flat) | 0.228 → 0.137 (narrows) |
| taper down its own length | 36% | 13% | 74% |
| outer-edge bow, % of chord | 9.84% | 6.52% | 4.46% |
| foot span @90% drop | 0.247 | **0.365** | **0.101** |

The **foot** is the discriminator — a 3.6× spread — which is why a necktie across the lapel opening is
fatal for this family, and why `jacket-lapel` styling was changed to NO TIE.

**`cp-trapezoid` drawing**, measured on the supplier's red overlay (which *is* the welt): taper ratio
**1.15, deeper at the OUTBOARD/armhole end**, depth/run 0.274 → 0.314.

**Legacy defect rate: 16% wrong across 25 graded.** Expect roughly that across the remaining 327.

---

## Blocked on a decision, not on effort

1. **`collar-round-53` vs `collar-large-round-53`** — the drawings differ by an ink-region mean of
   16.9/255, *lower* than the 20.2 caused by two 4px tip buttons elsewhere in the family. Identical
   100° callouts, identical 5.3 cm labels. **No photograph can ever separate them.** Same class of
   question for ~20 ladder-rung pairs.
2. **91 missing supplier drawings** behind 394 no-blueprint rows, including the whole
   `jacket/front-style/` field (13 files, 13 triage failures). The waistcoat equivalents are genuine
   1200×1200, so this is a supplier request, not research.
3. **Vercel env vars** — Stripe keys sit under the wrong names, so checkout returns 503 in production
   regardless of what ships. `.env.example` names every one.
4. **4 live-photo swaps** from the legacy audit (`--allow-swap` plus human approval).

---

## The two lessons that generalise

**The recurring defect is not bad reasoning about garments. It is sound reasoning that stopped at a
boundary.** A NO-TIE rule argued in full for shirt collars and never applied to waistcoats or lapels.
An `EXCLUSIVE_SHAPE_FAMILIES` mechanism built for exactly this problem, declaring two families out of
five. Six documented photography rules that lived only in reference files and never reached the prompt
string. A snapshot README asking the operator to refresh copies by hand. Every one was found by asking
*does the rule reach the artifact?* — never by re-reasoning about tailoring. When something is wrong,
check the pipeline boundary before rethinking the garment.

**A measurement is only evidence once you have checked that it measures the thing you named.** Earned
four times in one session on one option: a 139px depth inside a 140px box; two drawing measurements
disagreeing in *direction* because both read lapel edges instead of the welt; 290-of-290 "usable"
columns because the lapel ran through the box. Two measurements disagreeing is not a reason to pick
one — it means neither is validated. Crop the region and look at it.
